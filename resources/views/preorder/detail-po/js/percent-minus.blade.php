<script>
    // $(document).ready(function() {
    //     $('#diskon4-input').on('input', function() {
    //         let value = $(this).val().replace('%', '').replace('-', ''); // remove any existing '%' or '-' for proper checking
    //         let numericValue = parseFloat(value);

    //         if (!isNaN(numericValue)) {
    //             if (numericValue >= 1 && numericValue <= 100 && !$(this).val().endsWith('%')) {
    //                 $(this).val(numericValue + '%');
    //             } else if (numericValue > 100 && !$(this).val().startsWith('-')) {
    //                 $(this).val('-' + numericValue);
    //             }
    //         }
    //     });
    //     $('#diskon5-input').on('input', function() {
    //         let value = $(this).val().replace('%', '').replace('-', ''); // remove any existing '%' or '-' for proper checking
    //         let numericValue = parseFloat(value);

    //         if (!isNaN(numericValue)) {
    //             if (numericValue >= 1 && numericValue <= 100 && !$(this).val().endsWith('%')) {
    //                 $(this).val(numericValue + '%');
    //             } else if (numericValue > 100 && !$(this).val().startsWith('-')) {
    //                 $(this).val('-' + numericValue);
    //             }
    //         }
    //     });
    //     $('#diskon6-input').on('input', function() {
    //         let value = $(this).val().replace('%', '').replace('-', ''); // remove any existing '%' or '-' for proper checking
    //         let numericValue = parseFloat(value);

    //         if (!isNaN(numericValue)) {
    //             if (numericValue >= 1 && numericValue <= 100 && !$(this).val().endsWith('%')) {
    //                 $(this).val(numericValue + '%');
    //             } else if (numericValue > 100 && !$(this).val().startsWith('-')) {
    //                 $(this).val('-' + numericValue);
    //             }
    //         }
    //     });
    // });
</script>